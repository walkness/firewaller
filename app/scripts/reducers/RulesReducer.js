import * as types from 'constants/ActionTypes';
import initialState from 'config/initialState';

export default function rules(state = initialState.rules, action) {
  switch (action.type) {

    default:
      return state;

  }
}
